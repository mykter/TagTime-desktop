branches:
  only:
    - master

dist: trusty
sudo: false

language: node_js
node_js:
  - 6

os:
  - linux
  - osx

addons:
  apt:
    packages:
      - rpm       # dist
      - xvfb      # testing
      - icnsutils # this + gm for electron-builder's icon work
      - graphicsmagick

cache:
  directories:
    - node_modules

after_success:
  - npm run dist
  - if [[ $TRAVIS_OS_NAME = "linux" ]]; then
      npm run coveralls;
    fi
